# Public URL of the persistent Habitat HTTP service.
HABITAT_API_URL=https://habitat-api.example.com

# Server-side bearer secret configured on the Habitat service.
# Never expose this as a NEXT_PUBLIC_* variable.
HABITAT_API_TOKEN=replace-me
