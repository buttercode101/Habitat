# Habitat examples

`simple.yaml` is a minimal local Habitat configuration.

Run it with:

```bash
habitat init --config examples/simple.yaml
habitat run
habitat status
```

For event integration, see `PROTOCOL.md` in the repository root.
